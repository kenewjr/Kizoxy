# Bot
TOKEN=inseryourtokenhere
EMBED_COLOR=#000001
SEARCH_ENGINE=youtube
LEAVE_EMPTY=120000

# Dev
OWNER_ID=

# Nodes
NODE_NAME=insertyournodehere
NODE_URL=0.0.0.0:2333
NODE_AUTH=youshallnotpass

# Spotify
spotifyClientID=""
spotifySecret=""

# PostgreSQL (optional — leave empty to use JSON file storage)
POSTGRES_HOST=
POSTGRES_PORT=5432
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

# Logging
# "pretty" = colored terminal output (dev)
# "json"   = structured JSON output (production / PM2)
LOG_FORMAT=pretty

# Error Webhook (Discord webhook URL for error notifications)
ERROR_WEBHOOK_URL=
